<div class="mx-auto md:w-2/3 w-full">
  <div class="mb-8">
    <h1 class="font-bold text-4xl">New Music</h1>
  </div>

  <%= ui.card class: "p-8" do %>
    <%= render "form", music: @music %>
  <% end %>

  <div class="mt-8">
    <%= ui.btn "Back to repertoire", url: repertoire_musics_path, variant: :ghost %>
  </div>
</div>
